#include<stdio.h>
#include<stdlib.h>
/*typedef struct complexe {
float re;
float im;
}complexe;

complexe * new_complexe(float r, float i) {
   complexe *c = malloc(sizeof(complexe));
   c->re = r;
   c->im = i;
   return c;
}
void print_complexe(complexe *p) {
   printf("%f + %fi\n", p->re, p->im);
}

complexe * addition_complexes(complexe *c1, complexe*c2)
{
  complexe *resultat = malloc(sizeof(complexe));
   resultat->re = c1->re + c2->re;
   resultat->im = c1->im + c2->im;
   return resultat;
}
int main()
{
    float i,r;
    printf("donner la partie reel");
    scanf("%f",&r);
     printf("donner la partie imm");
    scanf("%f",&i);
    complexe *c1 = new_complexe(r,i);
    complexe *c2 = new_complexe(r,i);
    complexe *sum = addition_complexes(c1, c2);

    printf("c1 = ");
    print_complexe(c1);

    printf("c2 = ");
    print_complexe(c2);

    printf("sum = ");
    print_complexe(sum);

}*/


