#include<stdio.h>
#include<stdlib.h>
/*void remplir_tab(int *tab, int n)
{
    for(int i=0;i<n;i++)
    {
        printf("donner l'element de la case %d :",i+1);
        scanf("%d",tab+i);
    }
}
void Affich_tab(int *tab, int n)
{
    for(int i=0;i<n;i++)
    {
        printf("%d \t",*(tab+i));
    }
}
int Som_tab(int *tab, int n)
{ int s=0;
    for(int i=0;i<n;i++)
    {
        s=s+*(tab+i);
    }
    return s;
}
void copie_tab(int *s, int *tab, int n)
{
    for (int i=0;i<n;i++)
    {
        *(s+i)=*(tab+i);

    }
}
void inverse_tab(int *t, int n) {
    int *P1 = t;
    int *P2 = t + n - 1;
    int VT;
    while (P1 < P2) {
        VT = *P1;
        *P1 = *P2;
        *P2 = VT;
        P1++;
        P2--;
    }
}
int main()
{
    int n=5;
    int so;
    int *tab = malloc(n * sizeof(int));
    int *s = malloc(n * sizeof(int));
    remplir_tab(tab,n);
    Affich_tab(tab,n);
    so=Som_tab(tab,n);
    printf("la somme est %d",so);
   copie_tab(s,tab,n);
   printf("\n");
    Affich_tab(s,n);
    inverse_tab(tab,n);
     printf("\n");
 Affich_tab(tab,n);


}*/



