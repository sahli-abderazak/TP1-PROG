#include<stdio.h>
#include<stdlib.h>
/*void carre(int*x)
{
    *x=(*x)*(*x);
    printf("%d",*x);
}
void lirepositif(int*y)
{
    do
    {
        printf("donner un entier positif");
        scanf("%d",y);
    }while((*y)<=0);
}
void disque(float rayon, float *diametre, float *perimetre, float *surface)
{
    *diametre=rayon*2;
    printf("le diametre est \n%f\n",(*diametre));
    *perimetre=(2*3.14)*(rayon);
    printf("le perimetre est \n%f\n",(*perimetre));
    *surface=3.14*(rayon)*(rayon);
    printf("la surface  est \n%f\n",(*surface));
}
void somme_diff(int x, int y, int * somme, int * diff)
{
    *somme=x+y;
    printf("la somme est %i",*somme);
    *diff=x-y;
     printf("la difference est %i",*diff);
}
void permuter (int *x, int *y)
{
int temp;
temp=*x;
*x=*y;
*y=temp;
}

int main()
{
    int somme,diff;
    float diametre,perimetre,surface;
    float rayon;
    int y=5;
    int x;
   carre(&x);
    lirepositif(&y);
    printf("donner le rayon");
    scanf("%f",&rayon);
    disque(rayon,&diametre,&perimetre,&surface);
    somme_diff(x,y,&somme,&diff) ;
    permuter (&x,&y);
    printf("valeur de x %i et la valeur de y est %i\n",x,y);


}*/
